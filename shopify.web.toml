name = "remix"
roles = ["frontend", "backend"]
webhooks_path = "/webhooks/app/uninstalled"

[commands]
# Prisma needs DATABASE_URL; use npm scripts (CLI spawns commands without a shell — inline `VAR= cmd` breaks).
predev = "npm run prisma:generate"
dev = "npm run prisma:migrate && npm exec remix vite:dev"
